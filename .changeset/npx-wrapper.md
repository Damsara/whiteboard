---
"whiteboard-skills": minor
---

One-word install: `npx whiteboard-skills`. The package now ships a bin that delegates to the skills.sh installer against `Damsara/whiteboard`, forwarding any flags (`--list`, `--skill`, `--agent`). The standard `npx skills@latest add Damsara/whiteboard` form still works and is documented as the from-source alternative.
